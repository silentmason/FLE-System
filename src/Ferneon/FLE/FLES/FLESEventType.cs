using System;

namespace Ferneon.FLE.FLES
{
    public enum FLESEventType
    {
        OnStart,
        OnUpdate,
        OnInteract,
        OnTriggerEnter,
        OnTriggerExit,
        OnDamage,
        OnCustomEvent
    }
}
